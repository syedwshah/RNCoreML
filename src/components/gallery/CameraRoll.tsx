/*simple interface where we have the add picture button, and we also import the CameraRoll module from React Native.*/
import React from 'react';
import {Box, Text} from 'react-native-design-utility';

const CameraRoll = () => {
  return (
    <Box f={1} center>
      <Text>CameraRoll</Text>
    </Box>
  );
};

export default CameraRoll;
